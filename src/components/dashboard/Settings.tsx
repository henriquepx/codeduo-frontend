import styled from 'styled-components'

const SettingsContainer = styled.div`

`

const Settings = () => {
  return (
    <SettingsContainer>
        <h1>Settings</h1>
    </SettingsContainer>
  )
}

export default Settings
